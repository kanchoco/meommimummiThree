<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Comments">

	<select id="selectCommentUser" resultType="CommentsVO">
		select c.comments_number,c.comments_time,c.comments_content,c.comments_writer_nickname,c.comments_post_number 
		from tbl_comments c,tbl_post p,tbl_user u where c.comments_post_number=p.post_number and c.comments_writer_nickname =u.user_nickname
	</select>
	
</mapper>